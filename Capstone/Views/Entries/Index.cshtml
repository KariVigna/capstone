@{
    Layout = "_Layout";
}

@using Capstone.Models

<h1></h1>

@if (Model.Count == 0)
{
    <p>nothing to do today!</p>
}
else
{
    @foreach (Entry entry in Model)
    {
        <div class="container p-2 bg-primary">
        @if (!entry.IsComplete)
        {
            <li>@entry.Kid.Name<br>
            @Html.ActionLink(@entry.Title, "Details", "Entries", new { id = @entry.EntryId }) @entry.Reward<img src="~/css/images/.png" width="30" height="30" alt=""> 
                    @using (Html.BeginForm())
                        {
                            @Html.CheckBoxFor(Entry => entry.IsComplete)
                            <input type="submit" value="Save" />
                        }
            </li>
        }else{
            
            <li>@entry.Kid.Name<br>
            <strike>@entry.Title</strike></li>
        }
        </div><br>
    }
        
}
<hr/>

@Html.ActionLink("Add New Task", "Create", "Entries")<br>

