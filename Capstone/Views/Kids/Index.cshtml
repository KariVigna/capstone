@{
    Layout = "_Layout";
}

@using Capstone.Models

<h1>All Kids</h1>

@if (Model.Count == 0)
{
    <p>nothing to do today!</p>
}
else
{
    @foreach (Kid kid in Model)
    {
        <li>@Html.ActionLink(@kid.Name, "Details", "Kids", new { id = @kid.KidId })</li>
    }
}